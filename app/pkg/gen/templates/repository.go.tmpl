// Code generated by niko-admin. DO NOT EDIT.
// Model: {{.FilePath}}
// Generate at: {{.Timestamp}}

package repository

import (
	"gorm.io/gorm"

	"github.com/niko-admin/niko-admin/internal/model"
)

// {{.Name}}Repository handles database operations for {{.Name}} model.
type {{.Name}}Repository struct {
	db *gorm.DB
}

// New{{.Name}}Repository creates a new {{.Name}}Repository.
func New{{.Name}}Repository(db *gorm.DB) *{{.Name}}Repository {
	return &{{.Name}}Repository{db: db}
}

// FindByID finds a {{.PackageName}} by its ID.
func (r *{{.Name}}Repository) FindByID(id string) (*model.{{.Name}}, error) {
	var item model.{{.Name}}
	err := r.db.Where("id = ?", id).First(&item).Error
	return &item, err
}

// Create inserts a new {{.PackageName}} record.
func (r *{{.Name}}Repository) Create(item *model.{{.Name}}) error {
	return r.db.Create(item).Error
}

// Update saves changes to a {{.PackageName}} record.
func (r *{{.Name}}Repository) Update(item *model.{{.Name}}) error {
	return r.db.Save(item).Error
}

// Delete removes a {{.PackageName}} by its ID.
func (r *{{.Name}}Repository) Delete(id string) error {
	return r.db.Where("id = ?", id).Delete(&model.{{.Name}}{}).Error
}

// List returns a paginated list of {{.Plural}}.
func (r *{{.Name}}Repository) List(page, pageSize int) ([]model.{{.Name}}, int64, error) {
	var items []model.{{.Name}}
	var total int64

	query := r.db.Model(&model.{{.Name}}{})
	if err := query.Count(&total).Error; err != nil {
		return nil, 0, err
	}

	offset := (page - 1) * pageSize
	err := query.Offset(offset).Limit(pageSize).Order("created_at DESC").Find(&items).Error
	return items, total, err
}
